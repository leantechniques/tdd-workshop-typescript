import request from 'supertest';
import {exchange} from './exchange.controller';

describe('exchange controller', () => {
  it('should call to exchange service', async () => {
    expect(undefined).toBe(undefined);
  });
});
