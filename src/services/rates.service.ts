import {Rate} from 'models/rate';
import {CoinType} from 'models/coin';

export function getRates(...args: CoinType[]): Rate[] {
  return [];
}

export function getRate(id: CoinType): Rate {
  return undefined;
}